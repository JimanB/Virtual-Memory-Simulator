# Virtual-Memory-Simulator
A C program that simulates virtual memory address translation for different page table architectures. Supports both single-level and two-level page tables, calculates memory hierarchy parameters, and provides interactive address translation with binary decomposition.
